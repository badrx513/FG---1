const { MessageEmbed } = require("discord.js");

module.exports = {
  name: "Pub",
  cooldown: 5,
  aliases: ["P"],
  description: "send NSFW Photos in the channel",
  async execute(message) {
      const textChannel = message.channel;
  }
}